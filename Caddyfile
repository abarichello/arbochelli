# localhost
arbochelli.download

templates

file_server {
    hide .git* tmp *.html .mscbackup
    browse template.html
}

log {
    output file /tmp/arbochelli.log {
        roll_size 1gb
        roll_keep 3
    }
}
