{
    # local_certs
    admin off
    email webmaster@arbochelli.me
}

log.{$DOMAIN:localhost} {
    reverse_proxy http://umami:3000
}

{$DOMAIN2} www.{$DOMAIN:localhost} {
    redir https://{$DOMAIN:localhost}{uri}
}

r.{$DOMAIN:localhost} {
    root * redirect
    redir /static/* https://{$DOMAIN:localhost}{uri}
    file_server {
        index redirect.html
    }
    handle_errors {
        respond "{http.error.status_code} {http.error.status_text}"
    }
}

{$DOMAIN:localhost} {$ONION_ADDR} {
    templates
    encode gzip
    header Onion-Location {$ONION_ADDR}

    import redirect/rewrite.txt
    redir /p/* https://r.{$DOMAIN:localhost}/?r={http.request.uri.path.1}

    try_files {path}.html {path}
    try_files {path}.ico /static/{path}

    file_server {
        hide .git* .mscbackup .vscode *.html *.sh *.code-workspace README.md redirect src static status tmp .env *ocker* *.lock *.yml Caddyfile site
        browse template.html
    }
    file_server /status/* {
        index status.html
    }
    file_server /static/* {
        browse template.html
    }

    handle_path /blog/* {
        redir /static/* https://{$DOMAIN:localhost}{path}
        rewrite * /blog/public{path}
        file_server
    }

    handle_errors {
        respond "{http.error.status_code} {http.error.status_text}"
    }

    log {
        level DEBUG
        format json
        output file /tmp/arbochelli.log {
            roll_size 200mb
            roll_keep 5
        }
    }
}
